# 1. 调用 BookController 的方法
** 示例：通过 REST 客户端调用 **
假设你的应用运行在 http://localhost:8080，以下是对应的 API 调用路径和方法：

```
POST http://localhost:8080/api/books
Content-Type: application/json

{
    "title": "新书名",
    "author": "作者名",
    "isbn": "1234567890",
    "publisher": "出版社",
    "publishDate": "2025-06-09",
    "status": "AVAILABLE"
}
```

增加图书（POST 请求）：

删除图书（DELETE 请求）：

更新图书信息（PUT 请求）：

查询所有图书（GET 请求）：

根据 ID 查询图书（GET 请求）：

2. 调用 UserController 的方法
示例：通过 REST 客户端调用
增加用户（POST 请求）：

查询所有用户（GET 请求）：

根据 ID 查询用户（GET 请求）：

删除用户（DELETE 请求）：

3. 调用 BorrowRecordService 的方法